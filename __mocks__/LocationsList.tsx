
import React from 'react';

const LocationsListMock: React.FC<{ locations: any[]; handleLocation: Function }> = ({ locations, handleLocation }) => {
  // Return null to mimic the behavior of not rendering anything
  return null;
};

export default LocationsListMock;
